{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run C++ (clean)",
      "type": "shell",
      "command": "clang++ -std=c++17 -O2 \"${file}\" -o \"${workspaceFolder}/bin/run\" && \"${workspaceFolder}/bin/run\"",
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$gcc"]
    }
  ]
}