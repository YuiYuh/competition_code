#include<bits/stdc++.h>
using namespace std;

const int MOD = 65521;
const long long N = 1e15 + 5;
const long long M = 10;
int n, m;


int main(){
	ios::sync_with_stdio(false);
	cin.tie(0), cout.tie(0);
	
	cin >> n >> m;
	if(m == 1) cout << 0 << endl;
	if(n <= 2) cout << 0 << endl;
	
	
	return 0;
}
//放弃了 不是我这个蒟蒻可以写的题目 
